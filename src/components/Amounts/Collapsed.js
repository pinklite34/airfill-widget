import React from 'react';
import CollapsedSection from '../UI/CollapsedSection';

const Collapsed = () => (
  <CollapsedSection onClick={() => null} type="amount">
    Selected amount
  </CollapsedSection>
);

export default Collapsed;
